before_script:
    - git clone https://github.com/syngan/vim-vimlint /tmp/vim-vimlint
    - git clone https://github.com/ynkdir/vim-vimlparser /tmp/vim-vimlparser
    - sudo add-apt-repository -y ppa:terry.guo/gcc-arm-embedded --allow-unauthenticated
    - sudo apt-get update
    - sudo apt-get install -y gcc-arm-none-eabi --allow-unauthenticated

script:
    - uname -a
    - which -a vim
    - vim --cmd version --cmd quit
    - sh /tmp/vim-vimlint/bin/vimlint.sh -l /tmp/vim-vimlint -p /tmp/vim-vimlparser -v plugin

